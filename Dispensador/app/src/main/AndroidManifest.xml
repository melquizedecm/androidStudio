<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ana.dispensador">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cubaActivity" />
        <activity android:name=".DeviceListActivity" />
        <activity android:name=".palomaActivity" />
        <activity android:name=".DeviceListActivity2" />
        <activity android:name=".whiskyActivity" />
        <activity android:name=".DeviceListActivity3" />
        <activity android:name=".charroActivity" />
        <activity android:name=".DeviceListActivity4" />
        <activity android:name=".cubacaguaActivity" />
        <activity android:name=".DeviceListActivity5"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>